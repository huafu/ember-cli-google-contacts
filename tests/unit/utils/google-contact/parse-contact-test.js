import googleContactParseContact from 'ember-cli-google-contacts/utils/google-contact/parse-contact';

module('googleContactParseContact');

// Replace this with your real tests.
test('it works', function() {
  var result = googleContactParseContact();
  ok(result);
});

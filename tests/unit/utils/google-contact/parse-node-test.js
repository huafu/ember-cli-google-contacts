import googleContactParseNode from 'ember-cli-google-contacts/utils/google-contact/parse-node';

module('googleContactParseNode');

// Replace this with your real tests.
test('it works', function() {
  var result = googleContactParseNode();
  ok(result);
});
